package com.process.validation;

public class Validator {

	/*
	 * This function checks if Path Exist. 
	 * Returns:
	 * false: when Path doesn't exist
	 * true: when path Exist
	 */
	public static boolean validatePath(String path){
		return true;   // Abstracted now !!
	}
}
